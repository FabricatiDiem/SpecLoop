# Implementation Plan: SpecKit Automation Loop

**Branch**: `002-speckit-automation` | **Date**: 2026-03-13 | **Spec**: /specs/002-speckit-automation/spec.md
**Input**: Feature specification from `/specs/002-speckit-automation/spec.md`

## Summary

This feature implements a SpecKit automation engine that orchestrates the entire development lifecycle—from epic definition to git commit—by looping through an `EPICS.md` registry. It introduces `GOALS.md` for project-wide mission context and automates the sequential execution of `/specify`, `/plan`, `/tasks`, `/analyze`, and `/implement` commands. The engine ensures consistency between specs, performs automated verification based on the project's `constitution.md`, and handles incremental commits.

## Technical Context

**Language/Version**: Python 3.11+, Bash/Shell
**Primary Dependencies**: `click` (CLI), `pydantic` (Data Parsing), `git` (CLI), `pyyaml`
**Storage**: Filesystem (`EPICS.md`, `GOALS.md`, `specs/`)
**Testing**: `pytest`
**Target Platform**: CLI
**Project Type**: Automation Tooling
**Performance Goals**: N/A (Orchestration-focused)
**Constraints**: Must operate within existing SpecKit command structures
**Scale/Scope**: Ability to process complex multi-task epics autonomously

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

1. **Reusable Agentic Enablement**: **PASS**. This is a core enablement tool for automated workflows.
2. **GitHub SpecKit Backbone**: **PASS**. It literally automates the SpecKit backbone.
3. **Modular Polyglot Architecture**: **PASS**. Python orchestrator calling Bash/Markdown commands.
4. **Automated Enablement**: **PASS**. This is the definition of automated enablement.
5. **Semantic Versioning & Governance**: **PASS**. Workflow updates will follow versioning rules.

## Project Structure

### Documentation (this feature)

```text
specs/002-speckit-automation/
├── plan.md              # This file
├── research.md          # Epic parsing & command orchestration research
├── data-model.md        # Epic & Goal data structures
├── quickstart.md        # How to run the automation loop
├── contracts/
│   └── epic-schema.md   # Markdown structure requirements for EPICS.md
└── tasks.md             # Generated by /speckit.tasks
```

### Source Code (repository root)

```text
src/
├── speckit_loop/
│   ├── engine/          # Orchestration logic
│   ├── parsers/         # EPICS.md and GOALS.md parsers
│   ├── git/             # Commit & branch management
│   └── cli.py           # entry point: speckit-loop
├── scripts/
│   └── loop-runner.sh   # Bash wrapper if needed
```

**Structure Decision**: Option 1 (Single project) is chosen. The automation engine will live in `src/speckit_loop/`.

## Complexity Tracking

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| N/A | N/A | N/A |
