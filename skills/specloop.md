---
description: "Start the autonomous SpecKit development loop"
---

# SpecLoop

1. Use the `@specloop` subagent to begin the autonomous implementation of features defined in `EPICS.md`.
2. Monitor the progress as it selects epics, creates branches, and implements code.
3. You can interrupt the loop at any time by stopping the agent.
