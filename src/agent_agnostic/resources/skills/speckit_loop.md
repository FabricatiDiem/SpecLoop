---
description: "Start the autonomous SpecKit development loop"
---

# SpecKit Loop

You are handing off to the autonomous orchestrator.

1. **Launch the Subagent**: You MUST use the `@speckit-loop` subagent.
2. **Goal**: The subagent will process `EPICS.md` and implement each pending feature using the full SpecKit lifecycle (Specify, Plan, Tasks, Implement).
3. **Execution**: Do not perform any work yourself. Hand off immediately.

`@speckit-loop Start the autonomous SpecKit loop. First, run 'uv run speckit-loop next' to find the next pending epic.`
